[pytest]
testpaths = tests
markers =
    gpu: needs a CUDA device and model weights (run with: pytest -m gpu)
    network: downloads a dataset or model
    slow: takes more than a few seconds
# A bare `pytest` must be safe on any machine, including one WITH a GPU.
addopts = -q --strict-markers -m "not gpu and not network and not slow"
