# Pinned to the environment used for the published measurements. The
# intervention hooks internal hidden states, so Transformer versions can change
# the measured activations and should remain pinned for reference comparisons.

torch==2.6.0
torchvision==0.21.0
transformers==5.4.0
accelerate==1.13.0
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3

datasets==4.8.4
Pillow==12.1.1
qwen-vl-utils==0.0.14
tqdm==4.67.3
huggingface-hub==1.8.0
pandas==2.3.3

# Required for supplementary statistical analyses.
statsmodels==0.14.2

# Optional. faiss-gpu makes centroid fitting much faster; without a visible
# FAISS GPU the code falls back to sklearn MiniBatchKMeans, which gives
# slightly different centers. The original fit's FAISS package version was not
# recorded, so no exact version is pinned here.
# Install a CUDA-compatible faiss-gpu build separately when refitting.

# Required for the registry entries that load quantised (medgemma, medgemma_27b,
# gemma3_27b, qwen_32b, qwen3_32b, llava). Not needed for the seven models with
# shipped centroid banks, which all load in bf16.
bitsandbytes==0.49.2
